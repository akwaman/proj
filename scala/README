scala hello.scala     run the program

scalac hello.scala    compile the program and run it
scala hello

javap HelloWorld.class    bytecode disassembler
------------------------------------------------------------------

import BigInt._
var big1 = BigInt("1223334444555512233344445555")

val s = for (x <- 1 to 10) yield x*x